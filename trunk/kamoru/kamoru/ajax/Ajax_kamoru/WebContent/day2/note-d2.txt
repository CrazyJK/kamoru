2009-11-24 Ajax Day2

패키지 만드는 방법
	JSON 표기법
		패키지명.클래스명 = function() {
			색인배열,연관배열
객체 만드는 방법
	한건 만드는건 JSON을 사용한 배열로 쓰라
	eval()함수
		연관배열은 함수형태로 해야 하므로 ()를 넣는다
		
Ajax 간단 로그인
	서버쪽 처리와 클라이언트 처리를 분리하여 작업
	서버에서 보내는 데이타는 결과 코드를 JSON표기법으로 보낸다.
	클라이언트에서는 결과를 가지고 DHTML을 사용하여 보여준다
	
이벤트에 관하여
	이벤트 발생 DOM 레벨 0, 1
	DOM 레벨 1
		html과 자바스크립트가 분리
	IE / 비IE에서의 이벤트 핸들링 차이
		addEvenetListener / attachEvent	
	이벤트 객체(클래스) static 메서드로 이벤트 핸들링을 처리

	이벤트 흐름
		캡쳐영역 - 비IE만 있어서 잘 안씀.
		버블영역
		공통적인 이벤트 처리는 parent에서 처리하고
		특정 이벤트만 child에서 하고,
		버블로 올라가는 걸 막기 위해
			stopPropergation. cancelBubble
		이벤트 객체를 받는 방법
		
		함수에 인자를 받을때 arguement 로 다 받을수 있다.
